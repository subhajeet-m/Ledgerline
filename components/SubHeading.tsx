export default function SubHeading({ subheading }: { subheading: string }) {
    return (
        <div className="text-sm font-medium text-gray-400 pb-3">
            {subheading}
        </div>
    );
}
